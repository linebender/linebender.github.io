[package]
name = "rust_demo"
version = "0.1.0"
authors = ["Raph Levien"]
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
# Add web-sys or other crates as needed for web APIs
# web-sys = { version = "0.3", features = ["console"] }
